1. При тестировании метода "capitilize" обнаружен один дефект:
    а) Если указать на вход слово, состоящее, из заглавных букв, то метод вернет слово, состоящее с первой заглавной, а
    остальными - строчными
2. При тестировании метода "is_empty" обнаружен один дефект:
    а) Если указать на вход строку, состоящую только из пробела/пробелов, то метод вернет True вместо False
3. При тестировании метода "list_to_string" обнаружен один дефект:
    а) Если указать на вход не список, а, например, строку, то возвращает ошибку AssertionError